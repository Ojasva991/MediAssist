# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Kept separate from requirements.txt so production/Render installs never
# pull in test tooling they don't need.

pytest>=8.0
httpx>=0.27
pytest-cov>=5.0
